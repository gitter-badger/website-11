(page "index.html"
      (:require [components.header :refer [generic-header]]))

(html
 (generic-header)

 (body :class "index"
       ;; banner
       (section :id "banner"
                (div :class "inner" (header (h2 "Clojure Taiwan"))
                     (footer
                      (img :src "http://clojure-conj.org/images/clojure.png" :height "149" :width "150" :alt "clojure")
                      )
                     )
                )
       ))
